#aliases
alias grep='grep --color'
alias ssh='ssh -v'
alias clear='clear && source $ZDOTDIR/.zshrc'   #pretends we have a new te
alias df='df -h'
alias maven=mvn
alias v='nvim'
alias fuck='sudo $(fc -ln -1)';alias redo='sudo $(fc -ln -1)'
alias vlock="echo fucked up for now, don\'t try it"
alias py='python'
alias py3='python3'
alias book='book=$(pwd)'        #bookmarking util, set a bookmark with 'bo
alias j='nvim $HOME/$DOCS/journals/$(date +%Y%m%B)/$(date +%d).md +'    # 
alias c="clear" #one-character clearing                                   
alias rm="rm -I"
alias gaga="git add **; git commit -v"  #quickly git-commit everything in 
alias kdecli="kdeconnect-cli"	#just a faster way of using kdeconnect from the command line
